# Runtime Data

This folder is reserved for local runtime data such as logs and temporary caches.

Generated files in this folder are ignored by Git through both the root
`.gitignore` and this folder's own `.gitignore`.
